{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "default",
  "description": "Default capability set for the main window — covers dialog + filesystem so the project save/open and image storage commands work, plus the HTTP plugin so the renderer can reach local servers (the JustWrite server and user-configured LLM providers) without browser CORS getting in the way.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "fs:default",
    {
      "identifier": "http:default",
      "allow": [
        { "url": "http://localhost:*" },
        { "url": "http://127.0.0.1:*" },
        { "url": "http://0.0.0.0:*" },
        { "url": "https://*" },
        { "url": "https://*.*" },
        { "url": "https://*.*.*" },
        { "url": "https://*.*.*.*" },
        { "url": "https://*.*.*.*.*" }
      ]
    }
  ]
}
