{
  "name": "gpu",
  "band": "gpu",
  "_doc": [
    "THE GPU BAND — 'which models write well on this box's GPU?'",
    "",
    "A standing question, not a one-off experiment: add a leg whenever a model is",
    "worth testing, run just that leg, and the summary still prints the whole band",
    "(older legs are recalled from bench-results/ and labelled with their date).",
    "",
    "  npm run bench -- --config scripts/bench/configs/gpu.json --legs gpu-gemma-26b",
    "  npm run bench -- --config scripts/bench/configs/gpu.json --missing",
    "  npm run bench -- --config scripts/bench/configs/gpu.json --report   (no run)",
    "",
    "LEG IDS ARE DURABLE — they are how a result is matched across runs. Renaming",
    "one orphans its history; add a new id instead."
  ],
  "book": "prj_sample_ninth_facet",
  "features": ["chat", "characterChat", "entitySweep", "critique", "writerAI.continue", "writerAI.rewrite"],
  "repeats": 2,
  "timeoutMinutes": 20,
  "sweepChapterCap": 3,
  "ensureIndex": true,
  "presetName": "Bench",
  "featureArgs": {
    "chat": { "question": "What is this book about, and who is the protagonist?", "k": 6 },
    "characterChat": { "question": "What do you want most right now, and what are you afraid of?" },
    "writerAI.continue": { "passageChars": 2000 },
    "writerAI.rewrite": { "passageChars": 1200 }
  },
  "legs": [
    {
      "id": "gpu-gemma-26b",
      "label": "BASELINE — Gemma 4 26B-A4B QAT, as the app resolves it today",
      "_why": [
        "The bar every other leg is measured against, and deliberately the ONLY leg",
        "with no launch overrides: it loads exactly the way the app would load it",
        "for you right now (computed fit + whatever tunes exist), so the baseline is",
        "'the system as it works', not a hand-pinned config. What it resolved to is",
        "recorded in the leg's load/measure output.",
        "Every seeded preset points at this model, and its weights are already on disk."
      ],
      "model": "gemma-4-26b-a4b-qat",
      "tunables": { "temperature": 0.7, "think": false },
      "launch": {},
      "llamaBench": { "p": [512, 2048, 8192], "n": 128, "reps": 2 }
    }
  ]
}
